# flags-2-env-interfaces

Data-only contracts for Canonical CLI flag to environment mapping used by ORESoftware runtimes. Schema: `schema/v1`. Generated TypeScript and Dart live under `generated/` and must stay types-only.
